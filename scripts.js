// Initialize Confetti.js
var confettiSettings = { target: 'canvas', max: 100, size: 1.5, rotate: true };
var confetti = new ConfettiGenerator(confettiSettings);
confetti.render();

// Add custom animations for roses and rose petals here using JS or CSS animations.
